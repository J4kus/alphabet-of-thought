precision: up to minutes
displacement from greenwich meridian hours: +0

Duration: 15:00
    Interval
        - Scheduled Interval
            - Cron
            - Batch
            interval: 24 hours
            - ...
        start time: now
        interval: 5 minutes
        number of intervals: 1
    Milestone
        ... set flag
        Time: 15:00
        Date
            - 2026-03-15
            precision: no time component
        Date And Time
            - 2026-03-15 12:00
            precision: no seconds
        Precise Date And Time: 2026-03-15 12:00:00:00000
            precision: microseconds
        is end time and start time the same value: yes
    Condition
        end response event
        timeout duration: 0 seconds
        - Trade
            execution start precise date and time
            settlement end date
    Polling (Future)
        ... a low level placeholder for a result that is not ready yet
        end response event
        timeout duration: 30 seconds
        polling interval: 24 hours
    reference period: today
    start time
    end time
    average duration
    modal duration
    minimum duration
    maximum duration
    status
        - Time Set
        - Cleared
        - Triggered
        - Failed Trigger
        - Finalised
Workload
    pool permitted count (maximum worker count)
    result
    is done
    exception
    Thread
        Syncronous Thread
            - Lock
                ... only one thread may hold it at a time
            - Reentrant Lock
                ... the holding thread may acquire it repeatedly
            - Semaphore
                permit count
                ... allows a fixed number of threads through at once
            - Event
                ... one thread sets a flag, others wait on it
            - Condition
                ... a thread waits until another notifies it
            - Barrier
                ... releases all parties together once the count arrives
            routine count: 1
            risk
                - Race Condition
                    ... unsynchronised access to shared memory corrupts state
                - Deadlock
                        ... two threads each wait on a lock the other holds
        Asynchronous Thread (Wrapped Coroutine)
            wrapped synchronous thread
            is suspended: no
            suspension event
            awaiting event
            wrapped coroutine
    Parallel
        Queue
            - First In First Out Queue
            - Last In First Out Queue
            - ...
        Pipe
            ... a two ended connection between two processes
        Shared Memory Block
            ... a raw buffer mapped into several processes
        Manager
            ... serves proxies to shared Python objects
        worker count
        operation
            - map
            - apply
            - imap


